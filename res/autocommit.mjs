export default {
  // Specify the style of the commit message: 'simple', 'detailed'
  style: 'simple',

  // Prefix commit message with type (e.g., feat, fix, docs)
  typed: false,

  // Include scope in the commit message (e.g. core, auth, ui)
  scoped: false,

  // Include technical details in the commit message
  technical: false,

  // Specify the tense of the commit message: 'past', 'present'
  tense: 'present',

  // Include emoji in the commit message
  emoji: false,
};